/*=============Tabla:shopping_log_has_product
=======================================*/
/*Author: Luis Diego Rodriguez 2018148987*/
USE `pe`;

/*=============Getters===========================*/


DROP function IF EXISTS `get_id_product`;

DELIMITER $$
USE `pe`$$
CREATE FUNCTION `get_id_product` (pId INT)
RETURNS INTEGER
DETERMINISTIC
BEGIN


	DECLARE EXIT HANDLER FOR 1231 SELECT 'Variable cannot be set to that value.' Message;
	DECLARE EXIT HANDLER FOR 1232 SELECT 'Incorrect argument type to variable.' Message;
	DECLARE EXIT HANDLER FOR 1105 SELECT 'Unknown error encountered.' Message;



	DECLARE vcValue INT;
    SET vcValue = (SELECT id_product FROM shopping_log_has_product WHERE id_shopping_log = pId);
	RETURN vcValue;
END$$

DELIMITER ;


/*//////////////////////////////////////////////*/

DROP function IF EXISTS `get_id_shopping_log`;

DELIMITER $$
USE `pe`$$
CREATE FUNCTION `get_id_shopping_log` (pId INT)
RETURNS INTEGER
DETERMINISTIC
BEGIN


	DECLARE EXIT HANDLER FOR 1231 SELECT 'Variable cannot be set to that value.' Message;
	DECLARE EXIT HANDLER FOR 1232 SELECT 'Incorrect argument type to variable.' Message;
	DECLARE EXIT HANDLER FOR 1105 SELECT 'Unknown error encountered.' Message;



	DECLARE vcValue INT;
    SET vcValue = (SELECT id_shopping_log FROM shopping_log_has_product WHERE id_product = pId);
	RETURN vcValue;
END$$

DELIMITER ;
